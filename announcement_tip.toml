# Example announcement tips for Sofia TUI.
# Each [[announcements]] entry is evaluated in order; the last matching one is shown.
# Dates are UTC, formatted as YYYY-MM-DD. The from_date is inclusive and the to_date is exclusive.
# version_regex matches against the CLI version (env!("CARGO_PKG_VERSION")); omit to apply to all versions.
# target_app specify which app should display the announcement (cli, vsce, ...).

# Test announcement only for local build version until 2027-05-10 excluded
[[announcements]]
content = "This is a test announcement"
version_regex = "^0\\.0\\.0$"
to_date = "2027-05-10"

[[announcements]]
content = "Welcome to Sofia. Docs and releases: https://github.com/RuutChatCSM/sofia"
# Intentionally does not match released versions yet.
version_regex = "^0\\.0\\.0$"
