load("//:defs.bzl", "sofia_rust_crate")

sofia_rust_crate(
    name = "exec",
    crate_name = "sofia_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
