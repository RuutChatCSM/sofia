load("//:defs.bzl", "sofia_rust_crate")

sofia_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "sofia_goal_extension",
    integration_compile_data_extra = [
        "src/accounting.rs",
        "src/steering.rs",
    ] + glob(["templates/**"]),
)
