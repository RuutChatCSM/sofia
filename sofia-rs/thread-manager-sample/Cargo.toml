[package]
name = "sofia-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Sofia workspace dependency.
# Add new Sofia surface area to `sofia-core-api` instead of depending on
# additional `sofia-*` crates here.
sofia-core-api = { workspace = true }
tracing = { workspace = true }
